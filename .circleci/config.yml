version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build:
    environment:
      - CI_REGISTRY_IMAGE: 
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          command: docker build -t ${CIRCLE_PROJECT_USERNAME} .
          name: build

workflows:
  main:
    jobs:
      - build
